version: "3.9"

services:
  db:
    image: postgres:15-alpine
    container_name: phaseid-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-phaseid}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-phaseid}
      POSTGRES_DB: ${POSTGRES_DB:-phaseid}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -h 127.0.0.1 -p 5432 -U ${POSTGRES_USER:-phaseid} -d ${POSTGRES_DB:-phaseid}"
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  phaseid-api:
    build: .
    image: ${PHASEID_IMAGE:-phaseid/app:latest}
    container_name: phaseid-api
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_NAME: ${POSTGRES_DB:-phaseid}
      DATABASE_USER: ${POSTGRES_USER:-phaseid}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-phaseid}
      OUTPUT_DIR: /outputs
      INPUT_ROOT: /app
      PHASE_LIBRARY_PATH: /app/config/reference_phases.json
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
    volumes:
      - ./config:/app/config:ro
      - ./docs:/app/docs:ro
      - ./outputs:/outputs
    ports:
      - "${PHASEID_PORT:-8000}:8000"

  cod-loader:
    image: ${PHASEID_IMAGE:-phaseid/app:latest}
    container_name: phaseid-cod-loader
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_NAME: ${POSTGRES_DB:-phaseid}
      DATABASE_USER: ${POSTGRES_USER:-phaseid}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-phaseid}
    volumes:
      - ./db_raw:/app/db_raw:ro
    entrypoint: ["python", "scripts/load_cod.py"]
    profiles: [loader]
    command:
      [
        "--sqlite-path",
        "/app/db_raw/cod2205.sq",
        "--chunk-size",
        "1000"
      ]

volumes:
  postgres-data:
